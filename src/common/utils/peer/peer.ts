import { Peer } from 'peerjs';

// let peer = null;
// if (typeof window !== undefined) {
//   if (typeof window.navigator !== undefined) {
//     peer = new Peer();
//   }
// }
// const peer = new Peer();

// export default peer;
